name: Windows RDP via Pinggy
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Create RDP User
        env:
          USER_NAME: ${{ secrets.RDP_USER }}
          USER_PASS: ${{ secrets.RDP_PASSWORD }}
        run: |
          net user "$env:USER_NAME" "$env:USER_PASS" /add
          net localgroup administrators "$env:USER_NAME" /add
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "$env:USER_NAME"
          Write-Host "User created successfully."

      - name: Enable Remote Desktop
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1

      - name: Start Pinggy Tunnel
        shell: pwsh
        run: |
          Write-Host "Connecting to Pinggy..."
          Write-Host "---------------------------------------------------"
          # The '(n)' at the end disables the interactive dashboard
          # This forces Pinggy to output the URL as plain text in the logs
          ssh -p 443 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -R 0:localhost:3389 tcp@a.pinggy.io "(n)"